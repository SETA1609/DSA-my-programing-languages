/**
 * Array<number> | number[]
 * */
let numArray = [1,2,3,4,5];

/**
 * Array<number> | number[]
 * */
let numArrayRdm = [5,1,2,3,4];

/**
 * Array<string> | string[]
 * */
let strArray = ["a","b","c","d","e"];

/**
 * Array<string> | string[]
 * */
let strArrayRdm = ["z","b","a","e","d"];

let myObj = {
	"key":"value"
}

/**
 * Array<Object> | Object[]
 * */
let objArrayRdm = [myObj, myObj, myObj];

// push O(1) 
// pop O(1) 
// shift O(n)
// unshift O(n)
// concat O(n)
// slice O(n)
// splice O(n)
// sort O(n*log n)
